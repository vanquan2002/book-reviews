<div class="boxSearch mt-4">
  <span class="textSearch">{{text}}</span>
    <div class="row">
        {{#each books}}
        <div class="mainShow col-sm-6 col-lg-4">   
              <div class="card">
                <a href="/books/{{this.slug}}"><img class="card-img-top" src="../img/{{this.image}}" alt="{{this.name}}"></a>
                <div class="card-body">
                  <h5 class="card-title">{{this.name}}</h5>
                    <a class="detail" href="/books/{{this.slug}}">Xem chi tiết <i class="ti-hand-point-right"></i></a>
                </div>
            </div>
        </div>
        {{/each}}
      </div>
</div>
